# Database — use PostgreSQL connection string in production
DATABASE_URL=postgresql://user:password@localhost:5432/voice_form_assistant

# CORS — comma separated list of allowed origins
# For Chrome extensions, use: chrome-extension://YOUR_EXTENSION_ID
ALLOWED_ORIGINS=chrome-extension://your-extension-id-here,http://localhost:3000

# App
ENVIRONMENT=production